<Window x:Class="MVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="DeepSkyBlue" >
            <StackPanel Orientation="Horizontal">
                <Label Name="ipLbl" HorizontalAlignment="Center" FontSize="30">Header</Label>
            <Button Click="Button_Click" HorizontalAlignment="Right" FontSize="30">Change View</Button>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Name="MyGrid">
            
        </Grid>
        <Grid Grid.Row="2" Background="Orange" >
            <Label HorizontalAlignment="Center" FontSize="30">Footer</Label>
        </Grid>
    </Grid>
</Window>
